@model Projeto.Presentation.Mvc.Models.DependenteEdicaoModel

@{
    Layout = "~/Views/Shared/Layout.cshtml";
}

<h2>Atualizar dados do Dependente</h2>
<hr />

@using (Html.BeginForm())
{
    <!-- campo oculto -->
    @Html.HiddenFor(model => model.IdDependente)

    <label>Nome do Dependente:</label>
    @Html.TextBoxFor(model => model.Nome,
        new
        {
            @class = "form-control col-md-4",
            @autocomplete = "off",
            @placeholder = "Digite aqui"
        })
    <span class="text-danger">
        @Html.ValidationMessageFor(model => model.Nome)
    </span>
    <br />

    <label>Data de Nascimento:</label>
    @Html.TextBoxFor(model => model.DataNascimento,
        new
        {
            @class = "form-control col-md-4",
            @autocomplete = "off",
            @placeholder = "Digite aqui"
        })
    <span class="text-danger">
        @Html.ValidationMessageFor(model => model.DataNascimento)
    </span>
    <br />

    <label>Selecione o Funcionário</label>
    @Html.DropDownListFor(model => model.IdFuncionario, Model.ListaDeFuncionarios,
        "Selecione uma opção", new { @class = "form-control col-md-4" })
    <span class="text-danger">
        @Html.ValidationMessageFor(model => model.IdFuncionario)
    </span>
    <br />

    <input type="submit" value="Atualizar Dependente"
           class="btn btn-primary" />
}
